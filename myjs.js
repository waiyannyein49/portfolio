function myfunction()
        {
        	var x, text;
        	x=document.getElementById('number').value;
        	if(isNaN(x)|| x<1 || x>5)

        	{
        		text="sorry man it's over";
        	}
            else
            {
            	test="Thanks for your rating";
            }
